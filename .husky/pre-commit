#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "[pre-commit] Syncing glossaries..."
pnpm docs:sync || exit 1

# Stage updated glossaries if any
git add packages/*/GLOSARIO.md docs/GLOSARIO_MAESTRO.md 2>/dev/null || true

echo "[pre-commit] Done."